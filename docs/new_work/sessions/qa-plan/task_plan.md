# Task Plan: QA Plan — Outbound Engagement Engine

## Goal
Produce the executable QA plan the team runs to validate Phase 2 (backend + frontend,
all 12 plans) before pilot. The CTO defined QA as "end-to-end local testing + validating
production behavior."

## Deliverables
- `qa-plan.md` — the layered QA plan with concrete commands, payloads, and expected
  results per layer + per-plan focus + cross-cutting + sign-off checklist.
- `plan-09-staging-runbook.md` — the deferred Plan 09 NexHealth staging validation (4 flows).

## Inputs
- v3 verification (`../verification-phase2-v3/report.md`) — what's built + the fixed findings.
- Register (`../outbound-followups-and-gaps.md`) — deferred/known-open items.
- Fixes shipped: `../qa-prep-3-fixes/`, `../qa-prep-p2-tail/`.

## Status
Complete — see qa-plan.md.
