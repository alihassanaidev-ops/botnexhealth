[pytest]
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    rls: marks tests as requiring a real PostgreSQL database with RLS enabled
pythonpath = src
